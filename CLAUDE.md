# The Arena — moved

This file is a stub. Read `AGENTS.md` at the start of every session instead. It holds the current loop (`post_beta_hardening`), active guardrails, and pointers. History that used to live here is in `docs/` and `.arena/state.json`.
